# Immutable aggregate SQLite database used by the server.
# The runtime application must never read the raw source CSV files.
FOSTER_DATABASE_PATH=data/generated/foster_capacity.db

# Public build metadata returned by the health endpoint.
APP_VERSION=0.1.0

# Set this to the deployed Git commit SHA in production.
# APP_COMMIT_SHA=